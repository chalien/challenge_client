import React, { PropTypes } from 'react';
import Step from './Step'

const Wizard = (props) => {
  return (
    <div className="wizard">
      <Step></Step>
    </div>
  );
};

export default Wizard;

